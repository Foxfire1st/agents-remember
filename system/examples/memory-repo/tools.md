# Tools Example

Copy or rename this file to the memory layer's `system/tools.md` when scaffolding a memory repo.

Use this file as the active list of CLI commands, MCPs, branch workflow notes, and other checks the agent should reference. The derived path locations are documented in the same memory layer's `system/settings.md`.

## Coding Tools

### Code Quality

#### Repo 1
- npm run lint

#!/usr/bin/env bash
set -euo pipefail

repository_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repository_root"
exec uv --no-config run --project "$repository_root/mcp/pyproject.toml" --python 3.12 --extra dev \
  python -m agents_remember.testing.direct_runner "$@"
